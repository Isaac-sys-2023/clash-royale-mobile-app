import { StyleSheet } from "react-native";

const playerLeaderboardStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#1A1A1A',
    },
    scrollContainer: {
        flex: 1,
    },
    contentContainer: {
        padding: 16,
    },
})

export default playerLeaderboardStyles;